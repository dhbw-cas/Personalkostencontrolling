# Beispiel fuer eine lokale PostgreSQL-Instanz. Auf Sliplane wird der interne
# Hostname des privaten PostgreSQL-Service verwendet. Niemals echte
# Zugangsdaten in diese Datei eintragen.
DATABASE_URL=postgresql+psycopg://personalkosten:change-me@localhost:5432/personalkosten_dev
APP_VERSION=development
